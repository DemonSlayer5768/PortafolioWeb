---
const { class: className, id } = Astro.props;
---

<section
  id={id}
  data-section={id}
  class={`section ${className} max-w-6xl mx-auto  container mx-auto `}
>
  <slot />
</section>
