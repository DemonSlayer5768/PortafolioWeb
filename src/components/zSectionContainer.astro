---
const { class: className, id } = Astro.props;
---

<section
  id={id}
  data-section={id}
  class={`section ${className} max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 `}
>
  <slot />
</section>
